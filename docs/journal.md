# 구현 일지 — 만든 것 · 결정한 것 · 회고

> **[`PLAN.md`](PLAN.md) 와 역할이 다르다.**
> PLAN = *앞으로* 무엇을 왜 만들 것인가 (지도, 미래형).
> 이 문서 = *실제로* 무엇을 만들었고 무엇을 결정했나 (기록, 과거형).
> 계획이 바뀌면 PLAN 을, 결정·결과가 생기면 이 문서를 고친다.

---

## 1. 현재 상태

| 항목 | 값 |
|---|---|
| 진행 중 | **레이어 1 — 단일스레드 인메모리** |
| JDK | 21 LTS (배포판 무관) |
| 빌드 | 빌드툴 없이 `javac`/`java` 직접 |
| 실행 | `javac -d out (Get-ChildItem src -Recurse -Filter *.java).FullName` → `java -cp out kvdb.Main` |

**실제 소스 구조**
```
src/
  kvdb/
    Main.java        (REPL 진입점)
    store/           (저장 엔진)
```

**진행표** — ⬜ todo / 🟦 진행 / ✅ 완료

| 레이어 | 상태 | 회고 |
|---|---|---|
| 0 셋업 | ✅ | JDK 21 확정 · `javac`/`java` 직접 · 문서 5종. **배포판보다 버전이 중요했다** — Loom 실습이 21에 묶여 있어서. |
| 1 인메모리 | 🟦 | |
| 2 영속화 | ⬜ | |
| 3 네트워크 | ⬜ | |
| 4a 스레드 | ⬜ | |
| 4b 이벤트루프 | ⬜ | |
| 5 모니터링 | ⬜ | |
| 6 인덱스/복제 | ⬜ | |

**완료 시 남길 것**: 회고 1줄 + "만든 것 vs 표준(Redis/RocksDB/`java.util`): 무엇이 다르고 왜 저게 나은가" 1문단.

---

## 2. 설계 결정 기록

> 형식: **무엇을 골랐나 / 왜 / 언제 다시 볼까.** 마지막 칸이 핵심 — 나중에 재작업이 필요해지는 지점을 미리 표시해 둔다.

### 레이어 0 — 셋업

| # | 결정 | 선택 | 근거 | 재검토 |
|---|---|---|---|---|
| D1 | **JDK 버전** | **21 LTS** | 레이어 4a의 **Virtual Thread(Loom)** 가 21 정식. 17이면 "가상 스레드는 스택마저 힙에 둔다"([memory-model](memory-model.md) §8·§9.5) 대조 실습이 불가 | — |
| D2 | **JDK 배포판** | 아무거나 (Temurin 사용 중) | Temurin·Corretto·MS 모두 같은 OpenJDK·HotSpot 소스 → **학습 목적엔 무관**. 버전만 중요 | 배포 시 AWS 라면 Corretto 로 맞출 수 있음(선택) |
| D3 | **빌드** | 빌드툴 없이 `javac`/`java` | classpath·패키지·바이트코드를 **눈으로 보는 게** 학습. 프레임워크 금지 원칙 | 파일이 많아져 명령이 귀찮아지는 순간 = Gradle 도입 시점 |

### 레이어 1 — 인메모리

| # | 결정 | 선택 | 근거 | 재검토 |
|---|---|---|---|---|
| D4 | **패키지 구조** | `kvdb/` + `kvdb/store/`, 계층별·평평하게 | 레이어가 곧 패키지. Maven 레이아웃(`src/main/java`)은 빌드툴 없는 지금 이득 0 | 구현이 둘 이상 되면(직접 짠 해시맵 vs JDK, 4a vs 4b) 하위 패키지로 분할 |
| D5 | **없는 키 `get`** | **`null` 반환** + **value 에 null 저장 금지**를 규약으로 | null 을 못 넣게 하면 "없음"과 "null 저장됨"의 모호함이 **애초에 생기지 않는다**(Redis 모델 — value 는 항상 바이트 문자열, 빈 문자열은 되지만 nil 은 안 됨). `Optional` 은 조회마다 객체 할당 → 힙 관측이 지저분 | **레이어 4a** — `ConcurrentHashMap` 은 null 을 **금지**한다. 이유가 정확히 이 문제(동시 환경에선 `get`+`containsKey` 이중 조회가 race). 규약을 지켜뒀으면 전환 무사 |
| D6 | **key/value 공백** | **key 공백 금지 / value 는 줄 끝까지** | `split` 개수 제한만으로 `set k hello world` 가 됨. 따옴표·이스케이프는 지옥 | **레이어 3** — 아래 참조 |
| D7 | **명령어 대소문자** | 무시 (`SET`=`set`) | Redis 동작과 동일 | — |

**D6 보충 — 일부러 단순하게 간다**

레이어 3에서 "공백 있는 key 를 못 넣네 → 이스케이프를 넣을까 → **길이를 먼저 보내면 되잖아**" 로 이어지는 깨달음이 학습의 핵심. 지금 RESP 를 미리 구현하면 그 문제를 겪을 기회가 사라진다.

**Redis 도 같은 길을 걸었다**: inline command(공백 구분) → **RESP(길이 접두사)**.
`*3\r\n$3\r\nSET\r\n$1\r\nk\r\n$5\r\nhello\r\n` — "다음 5바이트가 값"이라 알려주니 구분자 탐색이 불필요하고 **binary-safe**(아무 바이트나 저장 가능, JPEG 도). 대가는 사람이 못 읽는 것 → 그래서 Redis 는 telnet 디버깅용으로 inline 도 **아직 지원**한다.

---

## 3. 관측 기록 (실측)

> PLAN 의 관측 실습을 **실제로 돌린 결과**를 남기는 자리. 캡처한 숫자가 있어야 나중에 비교가 된다.
> 레이어 5의 관통 실습 5개는 [`PLAN.md`](PLAN.md) 레이어 5 참조.

| 언제 | 무엇을 | 도구 | 결과 |
|---|---|---|---|
| — | (레이어 1: 대량 put 후 객체 분포) | `jmap -histo` | |
| — | (레이어 1: 힙 증가 추이) | `jstat -gc` | |

---

## 4. 막혔던 것 · 배운 것

> 해결한 문제와 그 원인. 같은 함정을 두 번 밟지 않기 위한 기록.

| 증상 | 원인 | 해결 |
|---|---|---|
| 터미널 `java -version` 이 17로 나옴 (21 설치했는데) | Machine PATH 에 17 경로가 하드코딩. Windows 는 **Machine PATH 를 User PATH 보다 먼저** 해석 | Temurin 21 설치 관리자가 Machine PATH·JAVA_HOME 을 21로 갱신. 진단은 `(Get-Command java).Source` ([toolchain](toolchain.md) §JDK 가 여러 개) |
